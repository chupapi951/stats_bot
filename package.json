{
  "name": "stats-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Telegram Mini App for order tracking and profit",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix webapp",
    "build:web":   "npm run build --prefix webapp",
    "dev:web":     "npm run dev --prefix webapp",
    "dev:server":  "npm start --prefix server",
    "start":       "npm run build:web && npm start --prefix server"
  }
}
